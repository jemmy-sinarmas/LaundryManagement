{
  "enabled": true,
  "name": "Build Verify on Agent Stop",
  "description": "Runs the production build across all workspaces after every agent session completes, to catch any broken build before the next task starts",
  "version": "1",
  "when": {
    "type": "agentStop"
  },
  "then": {
    "type": "runCommand",
    "command": "pnpm --filter @laundry-palu/shared run build && pnpm --filter @laundry-palu/api run build && pnpm --filter @laundry-palu/web run build",
    "timeout": 120
  }
}