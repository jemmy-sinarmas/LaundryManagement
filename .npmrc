; pnpm settings
; Use copies instead of symlinks for the content-addressable store on Windows.
; This avoids EPERM errors when tools try to create symlinks without
; Developer Mode enabled.
package-import-method=copy
